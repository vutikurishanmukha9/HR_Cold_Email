# Render Backend Environment Variables
# Copy these to Render Dashboard -> Your Service -> Environment

# Server Configuration
NODE_ENV=production
PORT=5000

# Database (copy from Render PostgreSQL service)
# Use the INTERNAL Database URL, not external
DATABASE_URL=postgres://user:password@host:5432/database

# JWT Secrets (use RandomKeygen.com -> Fort Knox Passwords)
JWT_SECRET=your-jwt-secret-key-minimum-32-characters
REFRESH_TOKEN_SECRET=your-refresh-token-secret-min-32-chars

# Encryption Key (MUST be exactly 32 characters)
# Use RandomKeygen.com -> CodeIgniter Encryption Keys
ENCRYPTION_KEY=exactly-32-characters-for-aes!!

# CORS - Your Render frontend URL
FRONTEND_URL=https://your-frontend.onrender.com
